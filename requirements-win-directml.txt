accelerate >= 0.32.1
basicsr
diffusers >= 0.29.2
gitpython
ninja
numpy
pillow
pyyaml
realesrgan
requests
transformers
wheel
wmi

# install general version of torch
torch == 2.3.1
torchvision == 0.18.1

onnxruntime-directml == 1.18.1  # use DirectML for DirectX supported GPU (NVIDIA/AMD/Intel)

optimum >= 1.16.2
optimum[onnxruntime-directml]   # optimum for onnxruntime-directml

